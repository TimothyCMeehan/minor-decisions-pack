
my_first_decision = {
	# Visual + AI housekeeping
	picture = {
		trigger = { culture = culture:norse }
		reference = "gfx/interface/illustrations/decisions/norse_break.dds" # norse only image
	}
	picture = { reference = "gfx/interface/illustrations/decisions/default_break.dds" } # default image

	# The title is localized from the key "my_first_decision"
	# description key (this links to localization)
	desc = my_first_decision_desc
		
	ai_check_interval = 180           # AI re-checks every 180 days (good default)

	# Who can see it
	is_shown = {
		always = yes
	}

	# Who can click it
	is_valid = {
		always = yes
	}

	# What happens when they click
	effect = {
		add_prestige = 50
		add_stress = -20
	}
}
